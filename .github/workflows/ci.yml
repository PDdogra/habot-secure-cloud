# ==============================================================================
# Candidate Name: Pranav Dogra
# Email: pranavdograa@gmail.com
# Workflow: Poka-Yoke Automated Security and Quality Gate
# ==============================================================================

name: Poka-Yoke Security & Quality Gate

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-gate:
    name: Fail-Closed Validation Gate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Scan for Hardcoded Secrets
        uses: trufflesecurity/trufflehog@main
        with:
          extra_args: --only-verified

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python Quality Tools
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black

      - name: Validate Python Formatting (Black)
        run: black --check .

      - name: Lint Python Code Structure (Flake8)
        run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Validate Terraform Syntax
        run: |
          terraform init -backend=false
          terraform validate
          terraform fmt -check
